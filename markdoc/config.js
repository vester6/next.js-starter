/** @type {import('@markdoc/markdoc').Config} */
const config = {
  variables: {
    site: {
      name: "Min Dokumentation",
      version: "3.0.0",
    },
    author: "Anna og Hans",
  },
};

export default config;
